import './index.css'

export default function App() {
  return (
    <main className="dev-shell">
      <h1>Email Filer</h1>
      <p>
        This page is only for local Vite dev. The Gmail extension is built with{' '}
        <code>vite build --config vite.extension.config.ts</code>.
      </p>
    </main>
  )
}
